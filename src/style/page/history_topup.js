import React, {
  StyleSheet
} from 'react-native'


const styles = StyleSheet.create({
  container: {
    flex: 1,
    marginTop: 65,
    paddingBottom: 5
  },
  loading: {
    alignItems: 'center',
    justifyContent: 'center'
  }
})

export default styles
